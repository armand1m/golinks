query getAllLinks {
  links(orderBy: ALIAS_ASC) {
    nodes {
      id
      url
      alias
      linkUsageMetrics {
        totalCount
        nodes {
          accessedAt
        }
      }
    }
  }
}

query searchLinks($search: String!) {
  searchLinks(search: $search) {
    nodes {
      id
      url
      alias
      linkUsageMetrics {
        totalCount
      }
      recentUsageMetrics {
        nodes {
          accessedAt
        }
      }
    }
  }
}

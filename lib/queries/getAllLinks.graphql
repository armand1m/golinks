query getAllLinks($first: Int = 50) {
  links(orderBy: ALIAS_ASC, first: $first) {
    nodes {
      id
      url
      alias
      linkUsageMetrics(first: 31, orderBy: [PRIMARY_KEY_DESC]) {
        totalCount
        nodes {
          accessedAt
        }
      }
    }
  }
}

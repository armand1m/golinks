mutation UpdateLink($id: UUID!, $patch: LinkPatch!) {
  updateLink(input: {id: $id, patch: $patch}) {
    link {
      id
      url
      alias
      isPrivate
      createdByEmail
      createdAt
    }
  }
}
